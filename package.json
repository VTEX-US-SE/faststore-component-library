{
  "name": "faststore-component-library",
  "version": "0.0.0",
  "private": true,
  "description": "Internal library of reusable components for FastStore v4 — VTEX Solutions Engineering (Rocketlane 1442908, phase P6).",
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "@changesets/cli": "^2.27.0",
    "typescript": "^5.6.0"
  }
}
